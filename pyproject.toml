[project]
name = "aichan"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["agent-service", "file-service", "hub-service", "tool-mcp-server"]

[dependency-groups]
dev = []

