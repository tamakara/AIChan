[project]
name = "aichan"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["core-service", "core-mcp-server", "file-service", "memory-service"]

[dependency-groups]
dev = []

